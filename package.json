{
  "name": "ext-link",
  "version": "0.0.1",
  "main": "lib/index.js",
  "repository": "https://github.com/WeSyncVideo/ext-link.git",
  "author": "Right2Drive",
  "license": "MIT",
  "private": false,
  "scripts": {
    "clean": "rimraf lib/",
    "clean:test": "rimraf lib/test/",
    "build": "npm run clean && tsc && npm run clean:test && mv lib/src/* lib/ && rmdir lib/src",
    "lint": "npm run lint:src && npm run lint:test",
    "lint:src": "npm run tslint -- \"src/**/*.ts\"",
    "lint:test": "npm run tslint -- \"test/**/*.ts\"",
    "test": "mocha ./test/**/*.spec.ts",
    "tslint": "tslint -c tslint.json -p tsconfig.json"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/chrome": "^0.0.70",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.5.3",
    "@types/redux": "^3.6.0",
    "chai": "^4.1.2",
    "eslint": "^5.2.0",
    "jsdom": "^12.2.0",
    "jsdom-global": "^3.0.2",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2",
    "testdouble": "^3.8.1",
    "ts-node": "^7.0.0",
    "tslint": "^5.11.0",
    "tslint-config-standard": "^7.1.0",
    "typescript": "^2.9.2"
  }
}
